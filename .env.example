# Copy to `.env` and fill in real values. `.env` is git-ignored.
# Astro exposes PUBLIC_*-prefixed vars to the browser.

# Google Analytics 4 measurement id. Analytics only loads in production builds
# AND only when this is set, so leaving it blank disables GA (e.g. in dev).
PUBLIC_GA_ID=G-XXXXXXXXXX
